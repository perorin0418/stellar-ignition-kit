# 画面設計ファイルの配置ルール

- このフォルダー配下には、画面名ごとに `html` ファイルと `md` ファイルを1組で作成する。
- ファイル名は同じ画面名でそろえ、拡張子のみを分ける。
	- 例: `ログイン画面.html`
	- 例: `ログイン画面.md`
- 画面が複数ある場合は、画面数分の `html` / `md` の組み合わせをこのフォルダー内に追加する。
- 画面がない場合は、このフォルダーはREADME.mdのみとする。
