# システム構成図

## 1. 文書の目的
システム全体の構成要素と接続関係を定義し、実装・運用の共通認識を作ることを目的とする。

## 2. システム構成図
<!-- AI_CONTEXT_START --><!--
- ネットワーク境界（VPC/Subnet）、接続方向、プロトコル（http/ssh）、コンポーネント（AWSサービス）を図示する。
- 図を作成する場合は、原則としてMermaid図を使用する。
- Mermaidで表現が困難な場合のみ、理由を明記したうえで代替形式を使用する。
--><!-- AI_CONTEXT_END -->
<!-- AI_FORMAT_START --><!--
```mermaid
graph LR
    subgraph VPC
        direction LR
        Subnet1
    end

    subgraph Subnet1
        direction LR
        Lambda1[Lambda Function1]
        VPC_Endpoint1[VPC Endpoint1]
    end

    Lambda1--&gt;|HTTP| VPC_Endpoint1
    VPC_Endpoint1 --&gt;|HTTP| S3[S3 Bucket]

    S3[S3 Bucket]
```
※コメントタグ内であるため、「>」は「&gt;」と表記している。実際にMermaid図を描画する際は「>」に置換すること。
--><!-- AI_FORMAT_END -->
<!-- AI_EDITABLE_START -->
```mermaid
graph LR
```
<!-- AI_EDITABLE_END -->
