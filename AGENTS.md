# AGENTS.md

## 目的
本ファイルは、AIエージェントが `doc` 配下のドキュメントを作成・更新する際に、既存の情報設計とテンプレート構成へ厳密に準拠させるための強制ルールである。

## 適用範囲
- `doc/**` の全ファイル
- 特に `doc/10_機能一覧/**` の機能別ドキュメント作成・更新

## 絶対ルール（MUST）
1. **ディレクトリ構成を変更しないこと**
   - 既存のトップレベル分類は固定とする。
     - `doc/00_全体概要/`
     - `doc/10_機能一覧/`
     - `doc/20_サービス統合/`
2. **命名規約を維持すること**
   - ファイル名・フォルダ名の接頭辞番号（例: `10_`, `20_`）を変更しない。
   - 既存テンプレートに存在する日本語ファイル名を改名しない。
3. **テンプレート構成を厳守すること**
   - 機能別ドキュメントは `doc/10_機能一覧/template/` と同一の階層・ファイルセットを基準にする。
   - 新規機能を追加する場合、`template` を複製した同等構成を作成する。
4. **編集可能領域のみ更新すること**
   - `<AI_READ_ONLY>...</AI_READ_ONLY>` 内は編集禁止。
   - `<AI_EDITABLE>...</AI_EDITABLE>` 内のみ編集可能。
   - 見出し番号・章構成（`## 1. ...` など）は変更しない。
5. **不足ファイルを作る際はテンプレート由来に限定すること**
   - 任意の新規ドキュメントを勝手に追加しない。
   - 追加可能なのは、テンプレートに対応するファイルのみ。
6. **事実不明な内容を断定記述しないこと**
   - 未確定事項は `80_未確定事項（TBD）.md` に記載する。
7. **図を作成する場合は、原則として Mermaid を使用すること**
   - 図を埋め込むドキュメントでは、基本形式を Mermaid 図とする。
   - Mermaid で表現困難な場合のみ、理由を明記したうえで代替形式を検討する。

## `doc/10_機能一覧` の標準構成
各機能フォルダは以下を最低限満たすこと。

- `10_要件定義/`
  - `10_業務要件.md`
  - `20_利用者・権限.md`
  - `30_機能要件.md`
  - `40_非機能要件.md`
  - `50_外部インターフェース.md`
  - `60_データ項目定義.md`
  - `70_リスク・課題.md`
  - `80_未確定事項（TBD）.md`
- `20_基本設計/`
  - `10_システム構成図.md`
  - `20_業務フロー.md`
  - `30_機能一覧.md`
  - `40_画面一覧.md`
  - `50_画面遷移図.md`
  - `60_画面設計/`
  - `70_テーブル定義書.md`
  - `80_帳票一覧.md`
  - `90_帳票レイアウト.md`
- `30_詳細設計/`
  - `10_コンポーネント構造図.md`
  - `20_処理シーケンス図.md`
  - `30_API詳細仕様.md`
  - `40_処理仕様書.md`
  - `50_例外処理方針.md`

## 作業手順（AIの実行順）
1. 対象が既存のどの分類（`00_全体概要` / `10_機能一覧` / `20_サービス統合`）かを特定する。
2. 対象ドキュメントがテンプレート準拠で存在することを確認する。
3. 変更は `AI_EDITABLE` ブロック内に限定して反映する。
4. 章立て・見出し番号・ファイル名・配置を維持できているか検証する。
5. 未確定情報は断定せず、必要に応じて `未確定事項（TBD）` へ移す。

## 禁止事項（MUST NOT）
- `template` 自体の破壊的変更（改名・章削除・構造変更）
- 接頭辞番号ルールを無視したファイル/フォルダ作成
- `AI_READ_ONLY` ブロックの編集
- 依頼のない新規分類（例: `30_運用設計`）の追加
- 既存文書の章番号体系を崩す変更

## 出力品質基準
- 追記内容は簡潔・具体・検証可能に記述する。
- 箇条書きを優先し、曖昧語（「適宜」「必要に応じて」）の多用を避ける。
- 図が必要な章では、図の目的・要素・凡例を文章で補足する。

---
このルールに反する提案は採用しない。AIは常に本ファイルを最上位制約として扱うこと。
